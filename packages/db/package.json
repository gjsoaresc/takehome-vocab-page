{
  "name": "@vocab/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "parse": "bun src/parse-pdf.ts",
    "migrate": "bun src/migrate.ts",
    "seed": "bun src/seed.ts",
    "bench": "bun src/bench.ts",
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5",
    "unpdf": "^1.3.2",
    "vitest": "^4.1.10"
  }
}
