# Postgres (docker compose up -d postgres)
DATABASE_URL=postgres://vocab:vocab@localhost:5434/vocab
# Used by db/api tests - a separate database is created/reset by the test setup
TEST_DATABASE_URL=postgres://vocab:vocab@localhost:5434/vocab_test
# API
API_PORT=3001
# Web (dev server proxies /api to this)
VITE_API_URL=http://localhost:3001
